<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/static/css/style.css">
</head>

<body>

    <div id="container">
        <div>
            {{#if errors}}
            <div class="notification loadingBox">
                Loading …
            </div>
            <div class="notification infoBox">
                <span>Info</span>
            </div>

            <div class="notification errorBox">
                {{#each errors}}
                <span>{{this}}</span>
                {{/each}}
            </div>
            {{/if}}
        </div>

        <nav>
            <div class="left-container">
                <ul>
                    {{#if user}}
                    <li><a href="/products/create">Add +</a></li>
                    <li><a href="/products">Home</a></li>
                    {{else}}
                    <li><a href="/products">Home</a></li>
                    <li><a href="/auth/login">Login</a></li>
                    <li><a href="/auth/register">Register</a></li>
                    {{/if}}
                </ul>
            </div>
            <div class="right-container">
                {{#if user}}
                <a href="/products/profile" class="log-out">{{user.username}}</a>
                <a href="/auth/logout" class="log-out">Logout</a>
                {{/if}}
            </div>
        </nav>
        {{{body}}}

    </div>

</body>

</html>